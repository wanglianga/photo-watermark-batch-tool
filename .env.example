# Docker 运行时环境变量示例
# 复制为 .env 后按需修改

# Linux X11 显示设置 (通常自动获取，无需修改)
DISPLAY=:0

# XDG 运行时目录 (通常自动获取)
# XDG_RUNTIME_DIR=/run/user/1000

# 视频组 ID (可通过命令 `getent group video | cut -d: -f3` 查看)
VIDEO_GID=44

# 音频组 ID (可通过命令 `getent group audio | cut -d: -f3` 查看)
AUDIO_GID=29
